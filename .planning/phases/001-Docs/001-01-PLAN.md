---
phase: 001-Docs
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/001-Docs/001-design-reconciliation.md
  - .planning/phases/001-Docs/001-page-source-matrix.md
  - .planning/phases/001-Docs/001-page-pattern-matrix.md
autonomous: true
requirements:
  - DOCS-001
  - DOCS-002
  - DOCS-003
  - DOCS-008
  - DOCS-009
  - DOCS-011
  - DOCS-012
must_haves:
  truths:
    - "All 89 current markdown files, including `content/docs/_support/another-file.md`, are assigned to a concrete rewrite or retention path."
    - "Every current docs page has at least one concrete `content/whitepapers/*.md` source path before content rewriting starts."
    - "Page-family pattern rules define reading-length, maturity, and selective markdown-plugin usage before any section rewrite."
  artifacts:
    - path: ".planning/phases/001-Docs/001-design-reconciliation.md"
      provides: "Current-tree versus design-scaffold coverage ledger"
    - path: ".planning/phases/001-Docs/001-page-source-matrix.md"
      provides: "Per-page whitepaper source mapping for the 89-page docs tree"
    - path: ".planning/phases/001-Docs/001-page-pattern-matrix.md"
      provides: "Family-specific writing, evidence, and markdown-pattern rules"
  key_links:
    - from: ".planning/phases/001-Docs/001-design-reconciliation.md"
      to: "docs/z00z_website-design.html"
      via: "Each current page is mapped to a design entry or explicit reconciliation note"
      pattern: "/learn/litepaper|/developers/migration-guides|/protocol/legal-architecture|/docs/ecosystem"
    - from: ".planning/phases/001-Docs/001-page-source-matrix.md"
      to: "content/whitepapers/*.md"
      via: "Primary and secondary corpus anchors per page"
      pattern: "content/whitepapers/"
    - from: ".planning/phases/001-Docs/001-page-pattern-matrix.md"
      to: "src/lib/content/markdown.ts"
      via: "Approved markdown-it and mdit plugin patterns per page family"
      pattern: "alert|tab|collapsible|mermaid|figure|footnote|dl"
---

<objective>
Build the execution ledger that prevents orphaned docs work across the current 89-page markdown tree.

Purpose: Lock the rewrite onto D-01 through D-11 before page-level execution starts.
Output: A design-reconciliation ledger, a per-page source matrix, and a page-pattern matrix that later plans can execute directly.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@docs/z00z_website-design.html
@.planning/phases/z00z_website-design.html
@src/lib/content/markdown.ts
@src/lib/content/html.ts
@config/content-pipeline.yaml
@.github/prompts/gsd-review-tasks-execution.prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the current-tree versus design ledger</name>
  <files>.planning/phases/001-Docs/001-design-reconciliation.md</files>
  <action>Create a Markdown table that lists every current markdown file under `content/docs/` plus `content/docs/_support/another-file.md`, maps each one to its design-scaffold role per D-02, and records how design-only routes are reconciled inside the current 89-page tree. Per D-03, explicitly mark routable stub pages as rewrite-from-scratch targets rather than protected copy. The ledger must also flag legacy evidence references that point to non-local paths such as `crates/*`, `docs/tech-papers/*`, `website/*`, or `/docs/ecosystem` so later plans do not preserve false live-evidence claims.</action>
  <verify>
    <automated>test "$(find content/docs -type f -name '*.md' | wc -l | awk '{print $1}')" -eq 89 &amp;&amp; while read -r file; do rg -Fq "$file" .planning/phases/001-Docs/001-design-reconciliation.md; done &lt; &lt;(find content/docs -type f -name '*.md' | sort) &amp;&amp; rg -q '/learn/litepaper' .planning/phases/001-Docs/001-design-reconciliation.md &amp;&amp; rg -q '/developers/migration-guides' .planning/phases/001-Docs/001-design-reconciliation.md &amp;&amp; rg -q '/protocol/legal-architecture' .planning/phases/001-Docs/001-design-reconciliation.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The reconciliation ledger covers all 89 current markdown files, names every design mismatch explicitly, and leaves no current docs page unassigned.</done>
</task>

<task type="auto">
  <name>Task 2: Build the per-page whitepaper source matrix</name>
  <files>.planning/phases/001-Docs/001-page-source-matrix.md</files>
  <action>Create a Markdown table with one row per current docs markdown file and columns for current page path, design role, primary `content/whitepapers/*.md` source, secondary sources, maturity posture, and source-note expectations. Per D-01, normalize legacy `docs/Z00Z-*.md` design evidence names to the current `content/whitepapers/*.md` corpus. Per D-07 and D-08, require concrete source paths for every page and note where a page may add local repo evidence only if that path exists in this repository.</action>
  <verify>
    <automated>test "$(rg -c '^\| content/docs/' .planning/phases/001-Docs/001-page-source-matrix.md)" -eq 89 &amp;&amp; ! rg -n 'missing primary|TBD' .planning/phases/001-Docs/001-page-source-matrix.md &amp;&amp; test "$(rg -c 'content/whitepapers/' .planning/phases/001-Docs/001-page-source-matrix.md)" -ge 89</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The source matrix covers all 89 current markdown files and gives every page at least one concrete whitepaper path plus a maturity/source-note expectation.</done>
</task>

<task type="auto">
  <name>Task 3: Define the family-level writing and renderer matrix</name>
  <files>.planning/phases/001-Docs/001-page-pattern-matrix.md</files>
  <action>Create a family-by-family matrix for `Home`, `Learn`, `Protocol`, `Developers`, `Network`, `Security`, `Research`, `Support`, `Use Cases`, `Legal`, and `_support`. Per D-04 through D-06, define the reading-length target, reader stance, and maturity/claim-hygiene rules for each family. Per D-09 through D-11, define which renderer-supported patterns are default, selective, or disallowed for that family: alerts, tabs, collapsible blocks, Mermaid, figures, footnotes, definition lists, task lists, containers, and attribute helpers. State that scaffold-only briefing headings must be replaced by reader-facing sections, with `## Evidence and Further Reading` required on routable pages.</action>
  <verify>
    <automated>test "$(rg -c '^\| (Home|Learn|Protocol|Developers|Network|Security|Research|Support|Use Cases|Legal|_support)' .planning/phases/001-Docs/001-page-pattern-matrix.md)" -eq 11 &amp;&amp; rg -q 'alerts' .planning/phases/001-Docs/001-page-pattern-matrix.md &amp;&amp; rg -q 'tabs' .planning/phases/001-Docs/001-page-pattern-matrix.md &amp;&amp; rg -q 'collapsible' .planning/phases/001-Docs/001-page-pattern-matrix.md &amp;&amp; rg -q 'Mermaid' .planning/phases/001-Docs/001-page-pattern-matrix.md &amp;&amp; rg -q 'footnotes' .planning/phases/001-Docs/001-page-pattern-matrix.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The family matrix covers every docs family, sets reading-length and claim-hygiene rules, and tells later plans when to use or avoid each enabled markdown pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper corpus -> docs rewrite plans | Source truth is compressed into public-facing docs instructions |
| design scaffold -> current docs tree | Legacy IA and evidence references must be reconciled against the real 89-page tree |
| markdown authoring -> renderer | Plugin-capable Markdown can introduce confusing or excessive structure if not constrained |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-01 | Tampering | page-source mapping | high | mitigate | Require one-row-per-page source mapping with concrete `content/whitepapers/*` paths before rewrites start. |
| T-001-02 | Spoofing | maturity and evidence status | high | mitigate | Record non-local evidence gaps and family-level maturity wording rules so later plans cannot present absent repo surfaces as live proof. |
| T-001-SC | Tampering | npm installs | low | accept | No package installs are part of this docs-planning slice. |
</threat_model>

<verification>
- The three planning artifacts exist and pass their row-count / coverage checks.
- Later plans can consume the artifacts without re-deriving page mapping, source mapping, or markdown-pattern policy.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- No current docs markdown file is orphaned.
- Every current page has a concrete whitepaper source path before rewrite execution begins.
- The execution rules for page length, evidence notes, maturity claims, and markdown patterns are explicit enough that later plans do not need to reinterpret D-01 through D-11.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-01-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md`, and continue immediately to `001-02-PLAN.md` in YOLO mode.
</output>
