---
phase: 001-Docs
plan: 6
type: execute
wave: 3
depends_on:
  - 001-01
  - 001-02
  - 001-03
files_modified:
  - content/docs/network/index.md
  - content/docs/network/overview.md
  - content/docs/network/aggregators.md
  - content/docs/network/validators.md
  - content/docs/network/watchers.md
  - content/docs/network/data-infrastructure.md
  - content/docs/network/onionnet.md
  - content/docs/network/data-availability.md
  - content/docs/network/checkpoint-anchors-zts.md
  - content/docs/network/node-operations.md
  - content/docs/network/status-explorer.md
  - content/docs/network/_meta.yaml
autonomous: true
requirements:
  - DOCS-001
  - DOCS-002
  - DOCS-003
  - DOCS-004
  - DOCS-005
  - DOCS-006
  - DOCS-007
  - DOCS-008
  - DOCS-009
  - DOCS-010
  - DOCS-011
  - DOCS-012
must_haves:
  truths:
    - "The network family explains operator roles, data movement, and observation boundaries without collapsing them into settlement authority."
    - "Every covered network page cites concrete whitepaper sources and keeps current-vs-target posture visible."
    - "The network sidebar matches the current page set and gives operators a readable route through role-specific docs."
  artifacts:
    - path: "content/docs/network/index.md"
      provides: "Operator-hub landing page with role-based reading order"
    - path: "content/docs/network/checkpoint-anchors-zts.md"
      provides: "Checkpoint/public-anchor page with authority-boundary framing"
    - path: "content/docs/network/_meta.yaml"
      provides: "Sidebar order for the actual current network pages"
  key_links:
    - from: "content/docs/network/index.md"
      to: "content/docs/network/_meta.yaml"
      via: "Hub ordering matches the current network page set"
      pattern: "overview|aggregators|validators|watchers|onionnet"
    - from: "content/docs/network/*.md"
      to: "content/whitepapers/*.md"
      via: "Evidence sections keep network claims grounded in the corpus"
      pattern: "content/whitepapers/"
    - from: "content/docs/network/checkpoint-anchors-zts.md"
      to: "content/docs/protocol/checkpoints.md"
      via: "Cross-link keeps checkpoint semantics and operator mechanics distinct"
      pattern: "/docs/protocol/checkpoints"
---

<objective>
Rewrite the network family so operator and runtime pages become readable, whitepaper-grounded docs for the current tree.

Purpose: Give later security, support, and use-case pages a stable network/operations reference without overstating implementation status.
Output: Rewritten current `content/docs/network/*` pages plus a synchronized network sidebar.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@.planning/phases/001-Docs/001-page-source-matrix.md
@.planning/phases/001-Docs/001-page-pattern-matrix.md
@content/docs/network/_meta.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the network hub and core role pages</name>
  <files>content/docs/network/index.md, content/docs/network/overview.md, content/docs/network/aggregators.md, content/docs/network/validators.md, content/docs/network/_meta.yaml</files>
  <action>Rewrite the network hub plus the first role pages per D-01 through D-08 so they establish the operator model clearly: network overview, aggregators, validators, and the separation between runtime role and settlement authority. Use the network-family recipe from the pattern matrix: role diagram first, maturity warnings early, then bounded detail. Keep the sidebar aligned to the actual page set, remove scaffold-only briefing sections, and end each routable page with `## Evidence and Further Reading` backed by the whitepaper corpus.</action>
  <verify>
    <automated>for f in content/docs/network/index.md content/docs/network/overview.md content/docs/network/aggregators.md content/docs/network/validators.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done &amp;&amp; rg -q 'checkpoint-anchors-zts' content/docs/network/_meta.yaml</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The network hub and first role pages are rewritten, evidence-linked, and ordered correctly in the sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite watchers, data, and privacy-ingress pages</name>
  <files>content/docs/network/watchers.md, content/docs/network/data-infrastructure.md, content/docs/network/onionnet.md, content/docs/network/data-availability.md</files>
  <action>Rewrite the watcher, data, OnionNet, and data-availability pages so they explain observation, ingress, privacy transport, and publication layers without overstating current implementation. Use collapsibles, comparison tables, and one compact diagram only when they improve scanability. Keep all claims grounded in `content/whitepapers`, explicitly label target versus current surfaces, and avoid non-local evidence paths.</action>
  <verify>
    <automated>for f in content/docs/network/watchers.md content/docs/network/data-infrastructure.md content/docs/network/onionnet.md content/docs/network/data-availability.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The middle network pages are rewritten into readable, current-repo-aware docs with explicit evidence and maturity posture.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the remaining operations pages</name>
  <files>content/docs/network/checkpoint-anchors-zts.md, content/docs/network/node-operations.md, content/docs/network/status-explorer.md</files>
  <action>Rewrite the remaining operations pages per D-04 through D-11 so they explain checkpoint anchors, node operations, and status/explorer concepts in current-tree terms. Preserve the distinction between operational visibility and settlement truth, use warnings or compact tables when they clarify non-claims, and end each page with concrete corpus evidence rather than absent mono-repo references.</action>
  <verify>
    <automated>for f in content/docs/network/checkpoint-anchors-zts.md content/docs/network/node-operations.md content/docs/network/status-explorer.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>All current network pages are rewritten into evidence-linked operator docs with clear authority boundaries and no leftover scaffold headings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper network concepts -> operator docs | Role compression can blur authority or maturity boundaries |
| network docs -> support/security/use-case docs | Later sections will rely on these operator-role definitions |
| markdown patterns -> operational clarity | Overuse of diagrams or collapsibles can hide important caveats |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-11 | Spoofing | operator-role and maturity claims | high | mitigate | Keep current-vs-target language explicit and require corpus-backed evidence on every network page. |
| T-001-12 | Tampering | checkpoint/authority explanations | high | mitigate | Cross-link protocol checkpoint pages and explicitly separate observation, publication, and settlement truth. |
| T-001-SC | Tampering | npm installs | low | accept | No package-install work is part of this network rewrite slice. |
</threat_model>

<verification>
- Covered network pages pass the reading-band, evidence-section, maturity-note, and scaffold-removal checks.
- The network sidebar reflects the current tree and preserves operator-role ordering.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- The network family becomes a readable operator and runtime reference for the current repo.
- Network pages remain whitepaper-grounded and honest about current versus target surfaces.
- No current network page is orphaned or left as a scaffold stub.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-06-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md`, and continue immediately to `001-07-PLAN.md` in YOLO mode.
</output>
