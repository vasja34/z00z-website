---
phase: 001-Docs
plan: 4
type: execute
wave: 3
depends_on:
  - 001-01
  - 001-02
  - 001-03
files_modified:
  - content/docs/developers/index.md
  - content/docs/developers/get-started.md
  - content/docs/developers/ai-agent-playbook.md
  - content/docs/developers/workspace.md
  - content/docs/developers/core.md
  - content/docs/developers/crypto.md
  - content/docs/developers/wallet.md
  - content/docs/developers/payment-requests.md
  - content/docs/developers/rpc.md
  - content/docs/developers/_meta.yaml
autonomous: true
requirements:
  - DOCS-001
  - DOCS-002
  - DOCS-003
  - DOCS-004
  - DOCS-005
  - DOCS-006
  - DOCS-007
  - DOCS-008
  - DOCS-009
  - DOCS-010
  - DOCS-011
  - DOCS-012
must_haves:
  truths:
    - "The developer onboarding and foundational builder pages explain the system without pretending this website repo contains the full Z00Z runtime."
    - "Every covered developer page uses whitepaper-grounded evidence plus current-repo paths only where those paths actually exist."
    - "The developers sidebar no longer points to the missing `migration-guides` route."
  artifacts:
    - path: "content/docs/developers/index.md"
      provides: "Builder-hub entry with current-repo-aware framing"
    - path: "content/docs/developers/get-started.md"
      provides: "Contributor start path that fits this repo's actual verification surface"
    - path: "content/docs/developers/_meta.yaml"
      provides: "Sidebar ordering without dead current-tree entries"
  key_links:
    - from: "content/docs/developers/index.md"
      to: "content/docs/developers/_meta.yaml"
      via: "Builder entry reflects the actual current-tree page set"
      pattern: "get-started|workspace|core|crypto|wallet|rpc"
    - from: "content/docs/developers/*.md"
      to: "content/whitepapers/*.md"
      via: "Evidence sections anchor builder explanations to the corpus first"
      pattern: "content/whitepapers/"
    - from: "content/docs/developers/get-started.md"
      to: "package.json"
      via: "Current-repo commands stay aligned with the actual verification commands available here"
      pattern: "npm run verify"
---

<objective>
Rewrite the foundational developer family so builder-facing docs are honest, navigable, and grounded in the current repo plus the whitepaper corpus.

Purpose: Provide a stable builder vocabulary before the runtime and tooling pages are rewritten.
Output: Rewritten onboarding and foundational current `content/docs/developers/*` pages plus a cleaned developers sidebar.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@.planning/phases/001-Docs/001-design-reconciliation.md
@.planning/phases/001-Docs/001-page-source-matrix.md
@.planning/phases/001-Docs/001-page-pattern-matrix.md
@content/docs/developers/_meta.yaml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the developer hub, entry path, and foundational map</name>
  <files>content/docs/developers/index.md, content/docs/developers/get-started.md, content/docs/developers/ai-agent-playbook.md, content/docs/developers/workspace.md, content/docs/developers/core.md</files>
  <action>Rewrite these builder-facing pages per D-01 through D-06 so they describe the Z00Z builder journey using the whitepaper corpus first and this repo's actual surfaces second. Replace legacy stub evidence that points to absent `crates/*` or tech-paper files with current-repo evidence only where it exists, such as `package.json`, `src/lib/content/markdown.ts`, or `npm run verify`; otherwise frame the page as design, target, or workflow guidance instead of live runtime documentation. Use the developer-family recipe: short answer first, exact commands or flows where locally verifiable, tabs or task lists where they materially reduce scanning cost, and a final `## Evidence and Further Reading` section.</action>
  <verify>
    <automated>for f in content/docs/developers/index.md content/docs/developers/get-started.md content/docs/developers/ai-agent-playbook.md content/docs/developers/workspace.md content/docs/developers/core.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done &amp;&amp; rg -q 'npm run verify' content/docs/developers/get-started.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The foundational developer pages are current-repo-aware, whitepaper-grounded, and free of dead legacy evidence references.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite crypto, wallet, payment, and RPC pages; then fix sidebar drift</name>
  <files>content/docs/developers/crypto.md, content/docs/developers/wallet.md, content/docs/developers/payment-requests.md, content/docs/developers/rpc.md, content/docs/developers/_meta.yaml</files>
  <action>Rewrite the crypto, wallet, payment, and RPC pages so they explain the concept, the workflow boundary, and the maturity posture without implying that absent mono-repo code is locally present. Per D-05 through D-11, use tabs, definition lists, and compact callouts only where they clarify the builder mental model. Then update `content/docs/developers/_meta.yaml` per the reconciliation ledger so the current tree no longer points to the missing `migration-guides` page; fold that intent into the actual pages that already exist in this tree.</action>
  <verify>
    <automated>for f in content/docs/developers/crypto.md content/docs/developers/wallet.md content/docs/developers/payment-requests.md content/docs/developers/rpc.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done &amp;&amp; ! rg -n 'migration-guides' content/docs/developers/_meta.yaml</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The foundational builder API, wallet, and RPC pages are rewritten and the sidebar matches the real current-tree page set with no dead route entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper/design guidance -> developer docs | Builder pages can accidentally overclaim local implementation status |
| current repo evidence -> builder instructions | Commands and file references must align with what exists in this repository |
| markdown patterns -> practical docs | Excess tabs or callouts can hide operational caveats |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-07 | Spoofing | developer implementation status | high | mitigate | Ban absent mono-repo evidence paths from rewritten builder pages and require explicit maturity wording. |
| T-001-08 | Tampering | builder instructions and verification commands | medium | mitigate | Anchor actionable commands to this repo's actual surfaces such as `package.json` and `npm run verify`. |
| T-001-SC | Tampering | npm installs | low | accept | No dependency installation work is part of this builder-doc rewrite slice. |
</threat_model>

<verification>
- Covered developer pages meet reading-band, evidence-section, maturity-note, and scaffold-removal checks.
- The developers sidebar no longer contains the dead `migration-guides` entry.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- Foundational builder pages are credible in the current repo context.
- Every covered page ends with concrete whitepaper evidence and no dead mono-repo evidence paths.
- The developer entry path is usable without requiring readers to infer missing local code.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-04-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md`, and continue immediately to `001-05-PLAN.md` in YOLO mode.
</output>
