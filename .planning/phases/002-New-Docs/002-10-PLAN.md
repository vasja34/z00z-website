---
phase: 002-New-Docs
plan: 10
type: execute
wave: 4
depends_on:
  - 002-02
  - 002-04
  - 002-06
  - 002-07
  - 002-08
  - 002-09
files_modified:
  - package.json
  - scripts/verify.sh
  - scripts/verify-search-coverage.mjs
  - scripts/verify-docs-phase-002.mjs
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/002-New-Docs/002-review-log.md
  - .planning/phases/002-New-Docs/002-doublecheck-report.md
  - content/docs/**/*.md
autonomous: true
requirements:
  - DOCS-009
  - DOCS-013
  - DOCS-014
  - DOCS-015
must_haves:
  truths:
    - "Phase 002 has a docs-specific verification gate that proves more than lint/build/search self-hit coverage."
    - "The final review loop is evidenced with at least three runs and a clean-run streak of two."
    - "A final workspace-first doublecheck report confirms TODO-to-context-to-plan coverage and repo-backed closeout claims."
  artifacts:
    - path: "scripts/verify-docs-phase-002.mjs"
      provides: "Phase 002 docs-specific acceptance checks"
    - path: ".planning/phases/002-New-Docs/002-review-log.md"
      provides: "Recorded multi-run planning/execution review evidence"
    - path: ".planning/phases/002-New-Docs/002-doublecheck-report.md"
      provides: "Final repo-backed verification report"
---

<objective>
Run the full-corpus consistency sweep, add the Phase 002 docs-specific verification gate, and complete the final review/doublecheck closeout.

Purpose: `npm run verify` alone is insufficient for Phase 002, so the phase needs an explicit docs acceptance surface plus the mandatory review and doublecheck evidence.
Output: Full-corpus normalization, docs-specific verify script, final review log, final doublecheck report, and synchronized roadmap/state metadata.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/002-New-Docs/002-TODO.md
@.planning/phases/002-New-Docs/002-CONTEXT.md
@.planning/phases/002-New-Docs/002-RESEARCH.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@.github/skills/doublecheck/SKILL.md
@package.json
@scripts/verify.sh
@scripts/verify-search-coverage.mjs
</context>

<coverage_contract>
plan_id: 002-10
task_ids:
  - CLOSEOUT-001
  - CLOSEOUT-002
  - CLOSEOUT-003
copied_task_rows:
  - `002-TODO.md:2775-2794` — `Acceptance Criteria`
  - `002-TODO.md:2795-2807` — `Verification Plan`
source_refs:
  - `/.github/prompts/gsd-review-tasks-execution.prompt.md`
  - `.github/skills/doublecheck/SKILL.md`
  - `scripts/verify.sh`
  - `scripts/verify-search-coverage.mjs`
  - `.planning/phases/002-New-Docs/002-TODO.md`
inputs:
  - all executed Phase 002 outputs
  - TODO acceptance and verification sections
outputs:
  - docs-specific verification script and package/script wiring
  - final review log
  - final doublecheck report
  - synchronized roadmap/state metadata
dependencies:
  - `002-02`, `002-04`, `002-06`, `002-07`, `002-08`, `002-09`
acceptance_tests:
  - a dedicated Phase 002 verification command proves word-count, evidence-link, TOC, icon-contract, and `Read Next` coverage
  - final review log records at least three runs and a two-clean-run streak
  - final doublecheck report contains verification ratings and no unresolved material contradiction
plan_artifacts:
  - verify script
  - review log
  - doublecheck report
task_artifacts:
  - repo verification wiring and closeout evidence files
blockers:
  - do not declare Phase 002 complete if the docs-specific verification gate cannot prove the TODO acceptance conditions
evidence_gate:
  - final closeout evidence explicitly proves TODO acceptance and verification bullets
not_recommendation_gate:
  - do not rely on `npm run verify` alone as the only acceptance proof
</coverage_contract>

<tasks>

<task type="auto">
  <name>Task 1: Add a docs-specific Phase 002 verification gate on top of the existing repo verify surface</name>
  <files>package.json, scripts/verify.sh, scripts/verify-search-coverage.mjs, scripts/verify-docs-phase-002.mjs</files>
  <read_first>.planning/phases/002-New-Docs/002-TODO.md, .planning/phases/002-New-Docs/002-RESEARCH.md, package.json, scripts/verify.sh, scripts/verify-search-coverage.mjs</read_first>
  <action>Extend the existing verification path rather than building a second verification system. Add a Phase 002 docs-specific verification script that checks the TODO acceptance requirements the current repo does not prove yet: required route existence, `toc: true`, `## Evidence and Further Reading`, direct whitepaper/source anchors, `Read Next` presence where required, difficulty/icon contract compliance, and defined word-count bands. Wire it into the existing verification entrypoints without duplicating the build/lint/search pipeline.</action>
  <verify>
    <automated>test -f scripts/verify-docs-phase-002.mjs &amp;&amp; rg -q 'verify-docs-phase-002' package.json scripts/verify.sh &amp;&amp; node scripts/verify-docs-phase-002.mjs</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <acceptance_criteria>
    - Phase 002 acceptance is no longer inferred from generic verify success.
    - The new gate extends existing verification rather than duplicating it.
  </acceptance_criteria>
  <done>Phase 002 has a deterministic local acceptance surface that proves the TODO contract materially better than the current repo state.</done>
</task>

<task type="auto">
  <name>Task 2: Run the final full-corpus consistency sweep and normalize shared patterns</name>
  <files>content/docs/**/*.md</files>
  <read_first>.planning/phases/002-New-Docs/002-TODO.md, .planning/phases/002-New-Docs/002-CONTEXT.md, content/docs/**/*.md</read_first>
  <action>Run a corpus-wide sweep across all public docs pages to normalize frontmatter shape, difficulty icons, `Read Next`, evidence sections, direct source anchors, section-home behavior, wording consistency, and live-vs-target posture after every family plan has landed. Fix any cross-family drift exposed by the new docs verification gate, and keep all corrections inside the existing docs/content/runtime seams rather than layering new abstractions on top.</action>
  <verify>
    <automated>node scripts/verify-docs-phase-002.mjs &amp;&amp; npm run verify</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <acceptance_criteria>
    - Cross-family drift is removed.
    - Corpus-wide acceptance passes on both the docs-specific gate and the repo verify surface.
  </acceptance_criteria>
  <done>The full public corpus is normalized and mechanically checkable against the Phase 002 acceptance contract.</done>
</task>

<task type="auto">
  <name>Task 3: Record final review evidence, run doublecheck, and synchronize roadmap/state metadata</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/002-New-Docs/002-review-log.md, .planning/phases/002-New-Docs/002-doublecheck-report.md</files>
  <read_first>.planning/phases/002-New-Docs/002-TODO.md, .github/prompts/gsd-review-tasks-execution.prompt.md, .github/skills/doublecheck/SKILL.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Record the repeated review/fix cycles in `002-review-log.md` with at least three review runs and a final clean-run streak of two. Then run a workspace-first one-shot doublecheck pass over the final Phase 002 evidence, including TODO coverage, verification-gate claims, and closeout metadata, and store the result in `002-doublecheck-report.md`. Only after both gates are green should `ROADMAP.md` and `STATE.md` be synchronized to the final executed state for Phase 002.</action>
  <verify>
    <automated>test -f .planning/phases/002-New-Docs/002-review-log.md &amp;&amp; test -f .planning/phases/002-New-Docs/002-doublecheck-report.md &amp;&amp; test \"$(rg -c '^## Review Run ' .planning/phases/002-New-Docs/002-review-log.md)\" -ge 3 &amp;&amp; rg -q 'Clean run streak: 2' .planning/phases/002-New-Docs/002-review-log.md &amp;&amp; rg -q 'VERIFIED\\|PLAUSIBLE\\|UNVERIFIED\\|DISPUTED\\|FABRICATION RISK' .planning/phases/002-New-Docs/002-doublecheck-report.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <acceptance_criteria>
    - Review-loop evidence and doublecheck evidence both exist.
    - Final roadmap/state metadata reflect the executed Phase 002 state, not the pre-execution planning snapshot.
  </acceptance_criteria>
  <done>Phase 002 closeout is backed by explicit review and verification evidence instead of implicit confidence.</done>
</task>

</tasks>

<verification>
- The docs-specific verification gate exists and runs locally.
- The full review loop and final doublecheck are both recorded with explicit evidence.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- Phase 002 has a deterministic local acceptance surface and explicit closeout evidence.
</success_criteria>

<output>
Create `.planning/phases/002-New-Docs/002-10-SUMMARY.md` when done.
</output>
