---
phase: 001-Docs
plan: 3
type: execute
wave: 2
depends_on:
  - 001-01
files_modified:
  - content/docs/protocol/index.md
  - content/docs/protocol/architecture.md
  - content/docs/protocol/settlement-model.md
  - content/docs/protocol/wallet-local-possession.md
  - content/docs/protocol/checkpoints.md
  - content/docs/protocol/assets-vouchers-rights.md
  - content/docs/protocol/smart-cash.md
  - content/docs/protocol/linked-liability.md
  - content/docs/protocol/privacy-threat-model.md
  - content/docs/protocol/cross-chain-rights.md
  - content/docs/protocol/tokenomics.md
  - content/docs/protocol/governance.md
  - content/docs/protocol/proof-of-useful-work.md
  - content/docs/protocol/post-quantum-migration.md
  - content/docs/protocol/_meta.yaml
autonomous: true
requirements:
  - DOCS-001
  - DOCS-002
  - DOCS-003
  - DOCS-004
  - DOCS-005
  - DOCS-006
  - DOCS-007
  - DOCS-008
  - DOCS-009
  - DOCS-010
  - DOCS-011
  - DOCS-012
must_haves:
  truths:
    - "Protocol pages explain settlement, privacy, rights, incentives, and governance as coherent system concepts rather than isolated stubs."
    - "Every covered protocol page cites concrete whitepaper sources and preserves current-vs-target claim hygiene."
    - "The protocol sidebar no longer points at the missing `/docs/protocol/legal-architecture` route."
  artifacts:
    - path: "content/docs/protocol/index.md"
      provides: "Protocol-hub landing page with system-model reading order"
    - path: "content/docs/protocol/checkpoints.md"
      provides: "Checkpoint/public-evidence explanation anchored to the corpus"
    - path: "content/docs/protocol/tokenomics.md"
      provides: "Economic and incentive framing with policy-draft caveats"
  key_links:
    - from: "content/docs/protocol/index.md"
      to: "content/docs/protocol/_meta.yaml"
      via: "Hub ordering matches the real protocol pages in the current tree"
      pattern: "settlement-model|wallet-local-possession|tokenomics"
    - from: "content/docs/protocol/*.md"
      to: "content/whitepapers/*.md"
      via: "Evidence section cites exact corpus files"
      pattern: "content/whitepapers/"
    - from: "content/docs/protocol/checkpoints.md"
      to: "content/docs/network/checkpoint-anchors-zts.md"
      via: "Protocol-to-network cross-link keeps settlement authority distinct from network operations"
      pattern: "/docs/network/checkpoint-anchors-zts"
---

<objective>
Rewrite the current protocol family into a coherent, evidence-linked architecture reference for settlement, privacy, rights, incentives, and governance.

Purpose: Establish the system-model pages that later network, developer, security, and use-case plans will cross-link against.
Output: Rewritten current `content/docs/protocol/*` pages plus a corrected protocol sidebar for the real tree.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@.planning/phases/001-Docs/001-design-reconciliation.md
@.planning/phases/001-Docs/001-page-source-matrix.md
@.planning/phases/001-Docs/001-page-pattern-matrix.md
@content/docs/protocol/_meta.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the protocol hub and settlement spine</name>
  <files>content/docs/protocol/index.md, content/docs/protocol/architecture.md, content/docs/protocol/settlement-model.md, content/docs/protocol/wallet-local-possession.md, content/docs/protocol/checkpoints.md</files>
  <action>Rewrite the hub and the main settlement-spine pages per D-01 through D-06 so they explain the protocol model in reader order: architecture, settlement authority, wallet-local possession, and checkpoints. Use the protocol-family recipe from the pattern matrix: one-screen thesis, a compact Mermaid or figure where it clarifies actor flow, invariants before detail, and explicit current-versus-target wording. Replace scaffold-only briefing sections, keep `## Evidence and Further Reading`, and preserve the distinction between settlement truth and supportive network services.</action>
  <verify>
    <automated>for f in content/docs/protocol/index.md content/docs/protocol/architecture.md content/docs/protocol/settlement-model.md content/docs/protocol/wallet-local-possession.md content/docs/protocol/checkpoints.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The protocol hub and settlement-spine pages explain the core system clearly, link to exact whitepaper sources, and avoid stub scaffolding or unsupported implementation claims.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the protocol rights and privacy extensions</name>
  <files>content/docs/protocol/assets-vouchers-rights.md, content/docs/protocol/smart-cash.md, content/docs/protocol/linked-liability.md, content/docs/protocol/privacy-threat-model.md, content/docs/protocol/cross-chain-rights.md</files>
  <action>Rewrite the rights, smart-cash, liability, privacy, and cross-chain pages using D-05 through D-11: lead with the bounded concept, use comparison tables or collapsibles where they reduce overload, and make the limits or non-claims explicit before optional depth. Keep the pages whitepaper-grounded, end each with a concrete evidence section, and keep cross-chain or liability claims framed as target architecture where current-repo proof is absent.</action>
  <verify>
    <automated>for f in content/docs/protocol/assets-vouchers-rights.md content/docs/protocol/smart-cash.md content/docs/protocol/linked-liability.md content/docs/protocol/privacy-threat-model.md content/docs/protocol/cross-chain-rights.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The protocol extension pages read as bounded explanations with explicit evidence and maturity framing, not as disconnected stubs.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite protocol economics and governance, then fix protocol navigation</name>
  <files>content/docs/protocol/tokenomics.md, content/docs/protocol/governance.md, content/docs/protocol/proof-of-useful-work.md, content/docs/protocol/post-quantum-migration.md, content/docs/protocol/_meta.yaml</files>
  <action>Rewrite the economics, governance, useful-work, and PQ-migration pages with explicit draft or migration posture per D-06. Use comparison blocks, compact tables, and footnotes only where they reduce ambiguity. Then update `content/docs/protocol/_meta.yaml` per the reconciliation ledger so the current tree no longer points to the missing `/docs/protocol/legal-architecture` route; route any legal-architecture expectation to the legal family instead of leaving a dead internal link.</action>
  <verify>
    <automated>for f in content/docs/protocol/tokenomics.md content/docs/protocol/governance.md content/docs/protocol/proof-of-useful-work.md content/docs/protocol/post-quantum-migration.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done &amp;&amp; ! rg -n 'legal-architecture' content/docs/protocol/_meta.yaml</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The protocol economics and governance pages are rewritten with honest maturity language, and the protocol sidebar no longer references a missing route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper protocol corpus -> docs pages | Deep technical material is compressed into shorter reference pages |
| protocol docs -> downstream network/developer/use-case docs | Terminology drift here propagates across later section rewrites |
| author markdown -> reader interpretation | Diagrams or collapsibles can obscure authority boundaries if used carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-05 | Tampering | protocol concept summaries | high | mitigate | Require evidence sections on every page and use the source matrix before introducing any new synthesis. |
| T-001-06 | Spoofing | protocol maturity and authority boundaries | high | mitigate | Keep explicit draft/target labels and remove dead links to missing protocol routes so readers cannot infer unsupported live capability. |
| T-001-SC | Tampering | npm installs | low | accept | No package installs are part of this protocol rewrite slice. |
</threat_model>

<verification>
- Each covered protocol page meets the reading band, evidence-section, maturity-note, and scaffold-removal checks.
- The protocol sidebar reflects the actual current-tree pages only.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- The protocol family becomes a coherent architecture reference rather than a set of placeholders.
- All covered protocol pages point to concrete whitepaper sources and keep target-vs-live wording honest.
- No current protocol page is orphaned, and no missing design route survives in the sidebar.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-03-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md`, and continue immediately to `001-04-PLAN.md` in YOLO mode.
</output>
