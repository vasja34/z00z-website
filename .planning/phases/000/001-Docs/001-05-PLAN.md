---
phase: 001-Docs
plan: 5
type: execute
wave: 3
depends_on:
  - 001-01
  - 001-02
  - 001-03
files_modified:
  - content/docs/developers/storage-hjmt.md
  - content/docs/developers/rollup-node.md
  - content/docs/developers/runtime-services.md
  - content/docs/developers/simulator.md
  - content/docs/developers/configuration-genesis.md
  - content/docs/developers/wasm-wallet.md
  - content/docs/developers/examples.md
  - content/docs/developers/api-reference.md
  - content/docs/developers/verification-tests.md
autonomous: true
requirements:
  - DOCS-001
  - DOCS-002
  - DOCS-003
  - DOCS-004
  - DOCS-005
  - DOCS-006
  - DOCS-007
  - DOCS-008
  - DOCS-009
  - DOCS-010
  - DOCS-011
  - DOCS-012
must_haves:
  truths:
    - "The runtime, tooling, and advanced builder pages explain current and target surfaces without inventing missing code evidence."
    - "Advanced developer pages still end in concrete whitepaper evidence and repo-local verification notes."
  artifacts:
    - path: "content/docs/developers/runtime-services.md"
      provides: "Runtime-surface explanation with honest maturity notes"
    - path: "content/docs/developers/verification-tests.md"
      provides: "Current verification story for this repo's docs and build workflow"
  key_links:
    - from: "content/docs/developers/verification-tests.md"
      to: "package.json"
      via: "Page references actual local verification commands rather than absent mono-repo tooling"
      pattern: "npm run verify"
    - from: "content/docs/developers/*.md"
      to: "content/whitepapers/*.md"
      via: "Evidence sections keep advanced builder pages anchored to the corpus"
      pattern: "content/whitepapers/"
---

<objective>
Rewrite the advanced developer, runtime, and verification family so the current repo has honest builder docs beyond onboarding.

Purpose: Complete the developer section with current-repo-aware runtime and tooling pages after the foundational builder path is stable.
Output: Rewritten advanced developer pages across runtime, tooling, API, examples, and verification topics.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@.planning/phases/001-Docs/001-design-reconciliation.md
@.planning/phases/001-Docs/001-page-source-matrix.md
@.planning/phases/001-Docs/001-page-pattern-matrix.md
@src/lib/content/markdown.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the runtime and environment pages</name>
  <files>content/docs/developers/storage-hjmt.md, content/docs/developers/rollup-node.md, content/docs/developers/runtime-services.md, content/docs/developers/simulator.md, content/docs/developers/configuration-genesis.md</files>
  <action>Rewrite these pages per D-01 through D-08 as whitepaper-grounded explanations of storage, runtime, simulation, and configuration concepts that remain honest about what is or is not present in this repo. Where current local commands or files exist, cite them precisely; otherwise describe the concept as target architecture or design intent. Use the developer-family pattern set selectively: task lists for workflows, tabs for role-specific reading, and concise callouts for maturity warnings. Remove scaffold-only briefing sections and end every routable page with `## Evidence and Further Reading`.</action>
  <verify>
    <automated>for f in content/docs/developers/storage-hjmt.md content/docs/developers/rollup-node.md content/docs/developers/runtime-services.md content/docs/developers/simulator.md content/docs/developers/configuration-genesis.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The runtime/environment pages are rewritten into honest concept-plus-workflow guides with corpus evidence and explicit maturity boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the advanced tooling, API, and verification pages</name>
  <files>content/docs/developers/wasm-wallet.md, content/docs/developers/examples.md, content/docs/developers/api-reference.md, content/docs/developers/verification-tests.md</files>
  <action>Rewrite the advanced tooling pages per D-04 through D-11 so they explain the builder-facing surface, current verification path, and examples strategy without leaking absent mono-repo evidence into this website repo. Use code-adjacent patterns only where they help: task lists, tabs, and definition lists for API surface explanation; no raw HTML-first shortcuts. Keep the verification guidance pinned to current local commands and current corpus evidence rather than to missing mono-repo scripts or packages.</action>
  <verify>
    <automated>for f in content/docs/developers/wasm-wallet.md content/docs/developers/examples.md content/docs/developers/api-reference.md content/docs/developers/verification-tests.md; do words=$(wc -w "$f" | awk '{print $1}'); test "$words" -ge 500 &amp;&amp; test "$words" -le 1700 &amp;&amp; rg -q '^## Evidence and Further Reading$' "$f" &amp;&amp; rg -q 'content/whitepapers/' "$f" &amp;&amp; rg -q 'Maturity:' "$f" &amp;&amp; ! rg -q '^(## (Page Brief|Reader Lenses|Section Lens|Navigation Links)|\+\+\+ Evidence and scaffold notes)' "$f" &amp;&amp; ! rg -q '(crates/|docs/tech-papers/|website/)' "$f"; done &amp;&amp; rg -q 'npm run verify' content/docs/developers/verification-tests.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The advanced developer pages are rewritten with honest examples, API, and verification guidance tied to the current repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper/runtime concepts -> advanced builder docs | Technical compression can drift into invented local implementation detail |
| local verification commands -> developer guidance | Builder instructions must match commands that really exist in this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-09 | Spoofing | advanced builder/runtime pages | high | mitigate | Forbid absent mono-repo evidence paths and keep target/live wording explicit on every advanced page. |
| T-001-10 | Tampering | verification guidance | medium | mitigate | Tie verification pages to `package.json` and local commands such as `npm run verify` rather than legacy scripts that do not exist here. |
| T-001-SC | Tampering | npm installs | low | accept | No dependency installation or package changes occur in this rewrite slice. |
</threat_model>

<verification>
- Covered advanced developer pages pass the reading-band, evidence-section, maturity-note, and scaffold-removal checks.
- Verification guidance points to current local commands rather than legacy mono-repo tooling.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- The advanced developer family becomes usable in the current repo context.
- Verification, examples, and API pages point to real local commands and concrete whitepaper sources.
- No advanced developer page remains a scaffold stub or implies missing local runtime code exists here.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-05-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md`, and continue immediately to `001-06-PLAN.md` in YOLO mode.
</output>
