---
phase: 001-Docs
plan: 10
type: execute
wave: 5
depends_on:
  - 001-09
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/001-Docs/001-review-log.md
  - .planning/phases/001-Docs/001-doublecheck-report.md
  - content/docs/**/*.md
autonomous: true
requirements:
  - DOCS-013
  - DOCS-014
  - DOCS-015
must_haves:
  truths:
    - "The mandatory review loop is documented with at least three YOLO review runs and two consecutive clean runs."
    - "A final doublecheck report exists and all material repo-backed contradictions are resolved before closeout."
    - "Phase metadata reflects the final validated 10-plan docs execution set."
  artifacts:
    - path: ".planning/phases/001-Docs/001-review-log.md"
      provides: "Evidence of the multi-run YOLO review loop"
    - path: ".planning/phases/001-Docs/001-doublecheck-report.md"
      provides: "Repo-backed factual verification results before closeout"
    - path: ".planning/ROADMAP.md"
      provides: "Phase plan list aligned to the final validated planning set"
  key_links:
    - from: ".planning/phases/001-Docs/001-review-log.md"
      to: ".github/prompts/gsd-review-tasks-execution.prompt.md"
      via: "Each review run records the prompt execution, findings, fixes, and clean-run streak"
      pattern: "Review Run"
    - from: ".planning/phases/001-Docs/001-doublecheck-report.md"
      to: "content/docs/**/*.md"
      via: "Final verification references concrete repo-backed claims and fixes"
      pattern: "content/docs/"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "Plan counts and current position remain synchronized after closeout prep"
      pattern: "10 plans|total_plans: 10"
---

<objective>
Run the mandatory review and verification closeout loop for the full docs rewrite phase, then synchronize the planning metadata.

Purpose: The phase brief requires repeated review/fix cycles and a final doublecheck before the docs work can be treated as complete.
Output: A reviewed and corrected docs tree with recorded evidence that the required review loops actually ran, plus synchronized roadmap/state metadata.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/001-Docs/001-CONTEXT.md
@.planning/phases/001-Docs/001-RESEARCH.md
@.github/prompts/gsd-review-tasks-execution.prompt.md
@content/docs/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run repeated YOLO review/fix cycles against the docs rewrite</name>
  <files>content/docs/**/*.md, .planning/phases/001-Docs/001-review-log.md</files>
  <action>Execute the `GSD-Review-Tasks-Execution` prompt inline against the active docs rewrite scope in YOLO mode at least 3 distinct times. Between runs, fix every material issue found. Keep recording the review sequence in `.planning/phases/001-Docs/001-review-log.md` with dated sections that capture reviewed scope, material findings, fixed files, and a `Clean run streak: N` line. Continue until at least 2 consecutive runs report no significant issues.</action>
  <verify>
    <automated>test -f .planning/phases/001-Docs/001-review-log.md &amp;&amp; test "$(rg -c '^## Review Run ' .planning/phases/001-Docs/001-review-log.md)" -ge 3 &amp;&amp; rg -q 'Clean run streak: 2' .planning/phases/001-Docs/001-review-log.md</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The required repeated review loop has been completed and evidenced with at least three runs and a two-clean-run streak.</done>
</task>

<task type="auto">
  <name>Task 2: Run final doublecheck and the repo verification gate</name>
  <files>content/docs/**/*.md, .planning/phases/001-Docs/001-doublecheck-report.md</files>
  <action>Run `doublecheck` in one-shot mode on the final docs rewrite evidence, focusing on repo-specific factual claims that materially affect correctness: page-to-source mapping, whitepaper grounding, route alignment, maturity wording, and review-loop completion claims. Store the output in `.planning/phases/001-Docs/001-doublecheck-report.md`, fix any material issues raised, then run `npm run verify` so lint and build confirm the docs render path still passes.</action>
  <verify>
    <automated>test -f .planning/phases/001-Docs/001-doublecheck-report.md &amp;&amp; rg -q 'VERIFIED\|PLAUSIBLE\|UNVERIFIED\|DISPUTED\|FABRICATION RISK' .planning/phases/001-Docs/001-doublecheck-report.md &amp;&amp; npm run verify</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The docs rewrite has both iterative review evidence and a final verification pass before phase completion.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize roadmap and state with the validated closeout set</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/001-Docs/001-review-log.md, .planning/phases/001-Docs/001-doublecheck-report.md</files>
  <action>After the review loop and doublecheck pass cleanly, update `ROADMAP.md` and `STATE.md` so they reflect the final validated 10-plan structure, the current position, and the existence of closeout evidence artifacts. Keep the plan list aligned to the exact canonical filenames and make the next operator action unambiguous.</action>
  <verify>
    <automated>rg -q '\*\*Plans:\*\* 10 plans' .planning/ROADMAP.md &amp;&amp; test "$(rg -c '001-..-PLAN.md' .planning/ROADMAP.md)" -eq 10 &amp;&amp; rg -q 'total_plans: 10' .planning/STATE.md &amp;&amp; rg -q '001-review-log.md' .planning/phases/001-Docs/001-doublecheck-report.md .planning/phases/001-Docs/001-review-log.md &gt;/dev/null</automated>
    <review_gate>Run `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times for the current plan scope, fix all issues and warnings between passes, and stop only after at least 2 consecutive runs report no significant issues.</review_gate>
  </verify>
  <done>The planning metadata and closeout artifacts are synchronized with the final validated docs plan set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final docs claims -> shipped trust surface | Final public docs claims must survive repeated review and source verification |
| review results -> completion decision | The phase must not soft-approve itself without clear review evidence |
| closeout evidence -> roadmap/state metadata | Planning metadata must reflect the validated end state, not an earlier draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-001-19 | Repudiation | review-loop evidence | high | mitigate | Record distinct review runs and fixes in the review log and require a clean-run streak before completion. |
| T-001-20 | Tampering | final factual claims | high | mitigate | Run one-shot doublecheck on repo-specific claims before closeout and fix material findings. |
| T-001-21 | Repudiation | roadmap/state metadata | medium | mitigate | Synchronize plan counts and closeout evidence references only after review and doublecheck complete. |
| T-001-SC | Tampering | npm installs | low | accept | Final closeout uses existing repo commands only; no package installs are required. |
</threat_model>

<verification>
- At least three YOLO review passes were completed and the final sequence contains two consecutive no-significant-issue runs.
- Final repo-specific factual claims were doublechecked before closeout.
- `npm run verify` passes and planning metadata reflects the validated 10-plan set.
- The current plan scope has been reviewed with `/.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline in YOLO mode at least 3 distinct times; all issues and warnings were fixed between passes; and review stopped only after at least 2 consecutive runs reported no significant issues.
</verification>

<success_criteria>
- Mandatory review and doublecheck gates are satisfied with explicit evidence.
- Final repo metadata reflects the validated planning set and closeout artifacts.
- The docs rewrite phase is ready for execution follow-through or verification without ambiguity.
</success_criteria>

<output>
Create `.planning/phases/001-Docs/001-10-SUMMARY.md` when done
Then update `.planning/ROADMAP.md` and `.planning/STATE.md` to the final validated closeout state for `001-Docs`.
</output>
